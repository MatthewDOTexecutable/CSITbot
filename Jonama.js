const myFunction = () => {

};

export default myFunction

client.on('message', msg => {
    if (msg.content === '!snek') {
        message.react('message.guild.emojis.find("snek")');
    }
});
client.on('message', msg => {
    if (msg.content === '!snek') {
        message.react('message.guild.emojis.find("snek")');
    }
});